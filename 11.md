for i in range(100):
    forward(i)
    right(90)
for i in range(100):
    forward(i)
    right(91) # 91度產生旋轉效果
colors = ["red", "orange", "yellow", "green", "blue"]
for i in range(50):
    color(colors[i % 5])
    forward(50)
    right(72)
for _ in range(5):
    forward(100)
    right(144)
for size in range(100, 0, -20):
    for _ in range(4):
        forward(size)
        right(90)
def draw_square(size):
    for _ in range(4):
        forward(size)
        right(90)
draw_square(50)
draw_square(100)
for _ in range(360):
    forward(1)
    right(1)
def circle(radius):
    circumference = 2 * 3.14 * radius
    step = circumference / 360
    for _ in range(360):
        forward(step)
        right(1)
n = int(input("Sides? "))
angle = 360 / n
for _ in range(n):
    forward(50)
    right(angle)
# 簡化版示意
locs = [(-60, 0), (0, 0), (60, 0), (-30, -30), (30, -30)]
cols = ["blue", "black", "red", "yellow", "green"]
for i in range(5):
    penup(); goto(locs[i]); pendown()
    color(cols[i])
    circle(20) # 假設已有 circle 函式
for _ in range(10):
    color(random.choice(["red", "blue", "green"]))
    draw_square(50)
    right(36)
def draw_petal(size):
    for _ in range(2):
        for _ in range(90): # 1/4 circle
            forward(size)
            right(1)
        right(90)
draw_petal(2)
for _ in range(8):
    draw_petal(1)
    right(45)
import random
for _ in range(50):
    right(random.randint(0, 360))
    forward(20)
form = input("Shape (square/circle)? ")
col = input("Color? ")
color(col)
if form == "square":
    draw_square(100)
elif form == "circle":
    circle(50)
else:
    print("I don't know that shape!")
